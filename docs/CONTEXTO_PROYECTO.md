# CONTEXTO DEL PROYECTO — Servi (oficio_app)

**Fuente de verdad única y portable.** Pégalo en cualquier chat/agente nuevo para
dar contexto completo sin explorar archivo por archivo. Claude lo auto-carga vía
`@docs/CONTEXTO_PROYECTO.md` en `CLAUDE.md`; Codex debe leerlo explícitamente por
instrucción de `AGENTS.md`. Mantener actualizado al cerrar cada tanda.

**Última actualización:** 2026-08-19 · Estado: **Auditoría web COMPLETA + tanda de fixes/rediseño del panel proveedor** — la web (Next.js) fue mapeada/auditada por rol. Última tanda (reporte del propietario sobre el panel): **PR #97** (`99a8b77`) corrige que Estadísticas mostraba "Reseñas: 0" (leía analytics, cuya respuesta no trae reseñas → ahora usa `provider.totalReviews`) y que el Teléfono del perfil salía vacío (fallback a `user.phone`). **PR #98** (`bc6db83`) reorganiza Perfil: una sola tarjeta de estado compacta (plan + disponibilidad segmentada + verificación como badge) con "Planes disponibles" integrado y colapsable → mucho menos scroll. **PR #99** (`30cc1b9`) completa la inversión de tema para opacidades que faltaban (Mensajes/AccountMenu quedaban blancos en modo claro) y agiliza la transición entre secciones del panel (`mode="wait"` era lento). **PR #95** (squash `49adb69`, pulido panel proveedor): los charts de `/panel/estadisticas` (recharts) tenían ejes/tooltip hardcodeados en dark (inline SVG que la inversión global no alcanza) → en modo claro salían invisibles; nuevo hook `useChartTheme` los hace por-tema; + empty state con icono y sidebar con colapsado persistente. **PR #93** (squash `aee0064`, seguridad): se añaden `Content-Security-Policy-Report-Only` (mismo criterio que admin, no bloquea), `Permissions-Policy` (solo geolocation self) y HSTS a `web/next.config.ts`; la base ya era buena (SameSite=Lax+Secure, X-Frame-Options DENY, sin `dangerouslySetInnerHTML`, todos los `target=_blank` con `rel=noopener`). Además de T1/T3 (abajo), **PR #90** (squash `1d66e6a`, T2 hub del cliente): el botón de quitar favorito estaba inerte (ahora togglea `POST /favorites/:id` + eliminación animada + tarjetas clicables), las notificaciones no se marcaban leídas (ahora al abrir la pestaña se llama a `read-all` por userId) y el deep-link `?tab=` solo reconocía `referidos` (ahora mapea cada sección). **PR #91** (squash `71b69e4`, modales/transiciones): toasts (sonner) según el tema vía `ThemedToaster` (antes fijo en dark → chocaba en claro) y transición deslizante entre pasos del modal de planes. **PR #87** (squash `18930b6`, T1 funcional): el panel web ahora **persiste servicios de verdad** (antes `handleSave` era `(simulado)`) en `scheduleJson.services` con CRUD + imagen/precio/unidad y límites reales por plan; fotos respetan el perfil activo (`?type=`) con límite por plan (2/6/10) y **portada elegible** (endpoint backend nuevo `PATCH /provider-profile/me/images/:id/cover`, columna `isCover` ya existía, sin SQL); disponibilidad usa `PATCH /me/availability` (antes 400); el radar de `/buscar` ya renderiza el mapa (faltaba `<TileLayer>`); el modal Yape ya no queda detrás del de planes (z-index); `/payments/success` refleja el estado real de Mercado Pago; copia de planes 2/6/10. **PR #88** (squash `910c4f8`, T3 pulido visual/cliente): se retira el correo personal expuesto (footer+manual → oficial), `ProviderCard` muestra disponibilidad y sello de credenciales (datos ya existentes), `/buscar` gana estado vacío útil + skeleton-grid, y el badge Premium del perfil público usa icono lucide en vez de emoji. Antes, PR #79 (squash `4702be7`): los tabs del panel ya no colapsan PROFESIONAL→OFICIO (helper `resolvePanelType`), lo que corrige el 404 en cancelar/analytics/perfil y la confianza que quedaba en "solicitar"; error de cancelar ahora es amigable; el panel abre el selector con MercadoPago+Yape; el toggle "Mostrar Ofi" es visible para todos. Antes, PR #77 (squash `7d42d14`): onboarding de bienvenida reducido de 7 a 4 slides y filtros avanzados unificados a un solo botón (toggle "Buscar por radio"). CI 3 checks verdes, sin backend/SQL; ambos llegan a usuarios al recompilar el `.aab`. El Asistente WhatsApp F1–F4 con OpenWA (PR #69, `0993337`) sigue mergeado, aislado y apagado por feature flags hasta el smoke de salida autorizado. Captación de negocios con botón Admin + credenciales (PRs #61/#62) sigue en producción. Subastas, ofertas, referidos/monedas, agenda y cotización continúan OCULTAS de forma reversible; Carta y Catálogo siguen visibles solo para proveedores NEGOCIO.

---

## 1. Qué es

**Servi** = marketplace de servicios locales para ciudades intermedias del Perú
(electricistas, gasfiteros, peluquerías, restaurantes, etc.). Mercado base: Junín
(Huancayo). **Modelo:** clientes gratis; proveedores pagan suscripción
(GRATIS / ESTÁNDAR / PREMIUM). Un usuario puede ser cliente y además tener perfil
proveedor OFICIO y/o NEGOCIO — los tres perfiles son independientes.
**Enfoque actual (2026-07):** la app está en lanzamiento — se REDUJO la
superficie de funciones para simplificar la experiencia; prioridad en
proveedores de OFICIO (los NEGOCIO son secundarios). Subastas y ofertas
quedaron ocultas (ver §3), reactivables a futuro.

## 2. Apps, stack y despliegue

| App | Stack | Puerto dev | Despliegue |
|-----|-------|-----------|-----------|
| **mobile/** | Flutter 3.41.6 (Dart), provider (ChangeNotifier), go_router, dio | — | .aab a Google Play (recompilar para publicar) |
| **backend/** | NestJS 11 (TypeScript **ESM**), Prisma CLI 7.8 + `@prisma/client` 7.6 + PrismaPg | 3000 | Render (`oficio-backend.onrender.com`), `main` auto-deploy, health `GET /health` |
| **admin/** | Next.js 16 (TS + Tailwind **v3**) | 3001 | Vercel (`oficioadmin`) |
| **web/** | Next.js 16 (landing + panel proveedor + chat + perfil público) | 3002 | Vercel (`oficio_web`) |

**Infra:** Supabase PostgreSQL 16 + PostGIS · Upstash Redis (cache/throttle/cuota IA) · Cloudflare R2 / MinIO (storage) · Firebase FCM (push) · WebSocket Socket.IO (`events/`). Local: `docker-compose up -d` (Postgres+PostGIS, Redis 7.2, MinIO).

## 3. Módulos

**backend/src/** — `auth` (JWT, OTP, social) · `users` · `providers` (listado/detalle/analytics/nearby) · `provider-profile` (panel propio + coverage) · `professional-migrations` (migración Oficio→Profesional preservando `Provider.id`, PR #55) · `reviews` (GPS/QR) · `favorites` · `chat` · `menu` (carta) · `catalog` · `appointments` (agenda) · `quotations` (cotización) · `coverage` (alcance por distrito) · `offer-posts` ⛔OCULTO · `subastas` ⛔OCULTO · `referrals` (canje monedas→plan) · `payments` (Yape + MercadoPago; PlanRequest DEPRECADO) · `trust-validation` · `user-reports` · `admin` (+services: dashboard/trust/reports/payments) · `ai-assistant` ("Ofi", aislado) · `whatsapp-assistant` (F1–F4: HMAC OpenWA, dedup, STOP/HUMANO, Ofi lectura-only, vínculo OTP HMAC y handover/DLQ ADMIN sin PII ni reintentos) · `events` (WS) · `firebase` (push) · `email` · `localities` · `common` (MinioService, provider-features.service, storefront.helpers, **feature-flag.guard**) · `generated` (cliente Prisma, regenera en CI — no editar). Schema: `backend/prisma/schema.prisma`.

**mobile/lib/features/** — `auth` · `providers_list` (listado+detalle+reseñas+filtros+radio) · `provider_dashboard` (panel proveedor, tabs) · `favorites` · `chat` · `menu` · `catalog` · `agenda` · `quotation` · `subastas` ⛔OCULTO · `offer_posts` ⛔OCULTO · `referrals` · `notifications` · `payments` · `trust_validation` · `localities` · `ai_assistant` · `showcase`. **mobile/lib/core/** — `constants` (app_colors, **feature_flags**), `theme` (app_theme_colors, theme_provider), `network` (dio_client), `router`, `services`, `errors`, `utils`, `widgets`. Cada feature: `data/` (repo) · `domain/` (models) · `presentation/` (screens/widgets/providers).

**Features ocultas (2026-07, código intacto):**
- **Subastas "ConfiServ" y ofertas:** reactivar con `FEATURE_SUBASTAS=true` / `FEATURE_OFERTAS=true` en Render, flags móviles y navs web/admin comentados. Tablas intactas; `/admin/offers`, `/admin/offer-reports` y `expireOffers` histórico siguen vivos.
- **Referidos/monedas, agenda y cotización (PR #37):** endpoints públicos devuelven 404 con `FEATURE_REFERIDOS`, `FEATURE_AGENDA` y `FEATURE_COTIZACION`; flujos internos salen temprano con la misma variable. Móvil usa `kReferidosEnabled=false`; Agenda/Cotización se ocultan dinámicamente desde el array backend `features`; web usa `NEXT_PUBLIC_FEATURE_REFERIDOS`. Ofi no declara herramientas de monedas y la fila `ai_knowledge_entries.topic = 'referidos_y_monedas'` quedó inactiva mediante SQL manual.
- **Carta y Catálogo:** continúan activos para `NEGOCIO`; `OFICIO` no recibe `carta_digital`/`catalogo` en `features` y sus endpoints de lectura/gestión lo bloquean. No existe flag actual para OFICIO: reactivarlo requiere PR backend con switches reversibles.

Runbook exacto, independiente por funcionalidad: [`REACTIVACION_FUNCIONALIDADES_OCULTAS.md`](REACTIVACION_FUNCIONALIDADES_OCULTAS.md). No revertir PR #36/#37 completos ni reactivar `PlanRequest`.

## 4. Convenciones NO negociables

- **Backend ESM**: imports locales SIEMPRE con extensión `.js` (`./x.service.js`). Sin `.js` = build roto.
- **Flutter feature-first** + **tema dinámico**: colores vía `context.colors` (AppThemeColors) para fondos/texto; acentos de marca vía `AppColors.*`; texto de acento sobre su tinte via `AppColors.tintOn(accent, c.isDark)`; glifo sobre fill sólido via `AppColors.onSolid(accent)`. NUNCA `AppColors.bgDark/textPrimary` estáticos ni `Colors.white/black` para superficies temáticas. Tema por defecto = **sistema**.
- **Providers globales**: los `ChangeNotifier` compartidos (Auth, Dashboard, Providers, Chat) son GLOBALES, no locales a un tab (evita `ProviderNotFoundException`). Su caché se limpia en logout (`_clearAll`/`attachAuth`) o filtra datos del usuario anterior.
- **Admin Tailwind v3** (no v4): `postcss.config.mjs` usa `tailwindcss: {}`.
- **Respuestas de auth**: login/verify-otp/social-login devuelven `userId`+`role` (móvil hace `data['userId'] as int` → si falta, crashea y no guarda sesión).

## 5. Base de datos y migraciones (LEER antes de tocar schema)

- **Baseline repo reparado:** `backend/prisma/migrations/0_init` único fue validado desde cero. Estado de `resolve --applied` en Supabase no confirmado; por eso producción NO usa `migrate deploy`. CI sincroniza solo su BD efímera con `db push`.
- **REGLA DURA:** cambios aditivos van por SQL idempotente en `backend/prisma/sql/`; el usuario aplica TODO SQL a Supabase **manualmente**. Ningún agente usa `execute_sql`, `apply_migration`, `migrate deploy`, `db push` ni `--force-reset` contra prod.
- Conexión: **pooler** de Supabase (`DATABASE_URL`, puerto 5432, `sslmode=require`). `npx prisma generate` local necesita `DATABASE_URL`/`DIRECT_URL` seteados (no conecta).
- Triggers en BD manejan `location_geog` (geography/GIST), `search_tsv` (tsvector/GIN) y `subscription_audit_log`. No usar `dbgenerated` con refs a columnas.
- **DateTime Prisma = timestamp SIN tz** (wall-clock UTC). Día Perú en SQL: `AT TIME ZONE 'UTC' AT TIME ZONE 'America/Lima'`. `now()` sí es timestamptz.
- **Tier gratis (diseñar para esto):** Render 512MB/0.1vCPU se duerme → Multer con límites + handlers globales `uncaughtException`/`unhandledRejection` que NO matan el proceso. Redis 10k cmds/día → solo cachear catálogos (`@CacheTTL 1h`) y respuestas IA. Supabase 500MB → logs a stdout, nada de tablas de logs masivas.

## 6. Flujo de trabajo (Branch Protection ACTIVA en main)

Cambios importantes: **rama nueva → commit → push a la rama → PR → CI verde (Backend/Mobile/Admin+Web) → squash-merge**. NUNCA push directo a main (lo bloquea). Triviales pueden ir directo. `gh` en `C:\Program Files\GitHub CLI\gh.exe`; token vía `git credential` (scope repo/workflow, **sin read:org** → usar `gh api -X POST/PUT/PATCH repos/L3x-00/OFICIO_APP/...` REST, no `gh pr create/edit`/merge que tocan campos de org). CI corre `npx prisma generate` (no commitear `generated/`). Repo: `L3x-00/OFICIO_APP`. Trailer de commit = agente real (`Claude <noreply@anthropic.com>` u `OpenAI Codex <noreply@openai.com>`).

## 7. Features en producción

- **Asistente WhatsApp F1–F4 con OpenWA** (PR #69, squash `0993337`, 2026-08-13): módulo `backend/src/whatsapp-assistant/`, aislado de Ofi. F1 verifica HMAC sobre cuerpo crudo, sesión, canal y deduplicación; aplica FAQ pública Servi, rechazo fuera de alcance, STOP y HUMANO. F2 usa Ofi solo para FAQ y búsqueda pública de proveedores, sin herramientas ni acciones de escritura. F3 vincula el número mediante OTP HMAC de 10 minutos y resuelve el contexto fresco por mensaje. F4 aporta handover y DLQ operativos bajo ADMIN, sin PII, replay ni reintento. Nunca persiste texto, teléfono, JID ni messageId en claro. Los SQL idempotentes `whatsapp_assistant.sql`, `whatsapp_assistant_links.sql` y `whatsapp_assistant_operations.sql` fueron aplicados manualmente por el propietario. F0 OpenWA fue configurado por el propietario. Auditoría previa de 2026-08-13 corrige carrera F3, guardas de handover, cuota pública y separación marca/IA; detalle en `docs/AUDITORIA_ASISTENTE_WHATSAPP_OFI_2026-08-13.md`. **Todas las flags continúan apagadas: no activar ni ejecutar smoke con salida hasta orden explícita del propietario.** Runbook: `docs/INTEGRACION_OPENWA_SERVI.md`.
- **Funcionalidades por categoría** (feature-gate por `Category.features`, herencia hija→padre): Carta y Catálogo (carrito → pedido WhatsApp) están activos solo para `NEGOCIO`; `OFICIO` queda bloqueado en filtro, lectura y gestión. Agenda (slots 30 min, recordatorio cron 24 h) y Cotización permanecen en código, pero ocultas por `FEATURE_AGENDA` / `FEATURE_COTIZACION`; sus CTA y entradas de panel no se muestran.
- **Referidos y monedas:** flujo, historial y tablas se conservan; con `FEATURE_REFERIDOS` apagado no acredita monedas ni expone endpoints, UI, notificaciones o herramientas Ofi. Las pruebas cubren ambos estados del flag.
- **Gating por plan**: carta/catálogo ítems GRATIS 5 / ESTÁNDAR 6 / PREMIUM ∞; agenda días activos/semana 1/3/7 (excede→402).
- **Alcance por distritos** (PR #28): `provider_coverage` = distritos EXTRA (el registrado siempre visible). Límite TOTAL por plan **GRATIS 1 / ESTÁNDAR 3 / PREMIUM 10**, misma provincia. **Gate en LECTURA** (`coverage.visibleInLocalities()`): extras solo si plan pago; al vencer quedan inertes pero se conservan. `syncCoverageToPlan()` (función suelta, try/catch, nunca revierte pago) siembra/recorta tras las 7 activaciones de plan. Endpoints `GET/PUT /provider-profile/me/coverage?type=`. Móvil: sección "Alcance" en tab Perfil.
- **Notificaciones** con discriminador (`AdminNotification.targetProfileType`/`metadata` JSONB para deep-link) — independencia por rol (cliente/OFICIO/NEGOCIO). Retención efectiva: leídas 5 días, no leídas 30 días; backend y móvil aplican el mismo criterio. Móvil informa de forma discreta cuando depura historial vencido.
- **Auth manual móvil:** un login fallido o un registro con correo duplicado permanece en su pantalla y muestra error amigable; el modo invitado solo se limpia después de autenticación exitosa. No confundir fallo de credenciales con usuario nuevo ni redirigirlo al onboarding.
- **Estados de proveedor importantes:** aprobación y rechazo viajan por notificación persistida + FCM. Móvil en foreground, resume o cold start consume una cola local y muestra overlay global; aprobación usa celebración ligera y rechazo conserva el motivo. Cada evento se marca consumido para no repetirse.
- **Admin operativo:** notificaciones con filtros de fecha en servidor, refresco Socket.IO (`notification`/`adminEvent`), reconexión, fallback visible de 60 s y cierre de socket en logout. Referidos/Recompensas están ocultos solo en navegación/dashboard; rutas e historial siguen vivos. Si un proveedor no tiene imágenes, Admin puede subir exactamente una imagen inicial; no existe CRUD, reemplazo ni borrado administrativo.
- **Búsqueda por radio** (PostGIS `getNearby`) respeta filtros activos; **listado por ubicación** jerárquica dept→prov→dist.
- **Toggles privacidad** (`showPhone`/`showWhatsapp`/`showExactLocation`). **Perfil público de usuario**. **Asistente IA "Ofi"** (guardrails PII/toxicidad/inyección, caché semántico, cuota atómica). Ofi es el asistente de Servi creado por el equipo de Servi; conserva 30 mensajes durante 12 h por conversación, permite iniciar chat nuevo y responde soporte con canales directos (WhatsApp, correo y footer), sin consumir IA para intenciones conocidas. Su conocimiento estable de plataforma se compila desde `servi.md` en `servi-platform-knowledge.ts`: datos públicos curados, URLs oficiales y límites de producto, nunca Markdown/instrucciones crudas. Para cada consulta recupera localmente como máximo 3 fragmentos relevantes / 3 200 caracteres, sin embeddings, red ni costo API extra; eso complementa — no sustituye — razonamiento del modelo, memoria, KB dinámica y herramientas autorizadas. La KB dinámica queda acotada a 30 entradas / 12 000 caracteres y no puede cambiar seguridad, alcance ni herramientas. Su aprendizaje interno usa memoria por usuario/proveedor y candidatos globales agregados, sin promover texto de chats ni PII. **Imágenes CDN**: 2 buckets R2 (público CDN + privado firmado), thumbnails Sharp.
- **Rediseño visual** "cálida artesanal": claro=crema `#FAF7F2`, oscuro=casi negro cálido `#0D0B08`; contraste AA vía onSolid/tintOn.
- **Landing web conectada a API real** (PR #31): hero con foto Huancayo legible en ambos temas (el hero conserva `force-dark-zone`); `solutions-section` con categorías padre reales + marquee infinito + geolocalización ("Ver servicios" → `/buscar?categoria&lat/lng` o fallback `provincia=Huancayo`); `/buscar` acepta deep-links; `providers-section` embebe el registro proveedor como **wizard de 6 pasos** (componente compartido `web/components/onboarding/provider-onboarding-form.tsx`, variantes full/wizard) con código de referido, borrador localStorage e invitados que se registran con Google al FINAL. El release PR #49 incorpora tema semántico adaptativo en ese bloque, elimina su `force-dark-zone` y actualiza la marca a Servi.
- **UX móvil integrada en PR #49:** bottom sheets de registro/filtros usan root navigator; Perfil unifica altas y estados OFICIO/NEGOCIO en "Mis perfiles"; clientes puros pueden ocultar el FAB de Ofi con `SharedPreferences`. Los flags de Subastas/Ofertas/Referidos permanecen apagados.
- **Panel web del proveedor (paridad con móvil, PR #87/#88, 2026-08-19):** el panel Next.js (`/panel/*`) edita perfil, disponibilidad, fotos y **servicios/productos** contra los mismos endpoints que el móvil. Servicios se persisten en `scheduleJson.services` (imagen/precio/unidad); fotos y disponibilidad se envían con `?type=` (perfil activo) y respetan el límite de galería por plan (2/6/10). La **portada** se elige con `PATCH /provider-profile/me/images/:id/cover` (única por transacción). Los resultados de `/buscar` muestran disponibilidad y sello de credenciales. El panel usa dark-hardcoded + inversión global de tema en `web/app/globals.css` (deuda anotada; no migrado a `dark:`).
- **Fix panel PROFESIONAL + pagos + Ofi (PR #79, squash `4702be7`, 2026-08-18):** los tabs del panel colapsaban todo lo no-NEGOCIO a `'OFICIO'` (`isNegocio ? 'NEGOCIO' : 'OFICIO'`), ignorando PROFESIONAL. Un proveedor migrado enviaba `type=OFICIO` → backend `findProviderByUser(userId,'OFICIO')`=null → **404 "Perfil de proveedor no encontrado"** en analytics/me/cancel-plan, y `profile_trust_section` leía `providerDataFor('OFICIO')`=null cayendo a "solicitar" pese a estar validado. Nuevo helper `resolvePanelType` (`provider_dashboard/presentation/panel_provider_type.dart`) distingue OFICIO/PROFESIONAL por el XOR de perfiles; aplicado en `panel_home_tab`, `panel_services_tab` y `profile_trust_section`. Además: (a) el error de cancelar plan interpolaba el `DioException` crudo (`$e`, texto con símbolos) → ahora extrae el mensaje de la `AppException` tipada del interceptor; (b) el panel saltaba directo a `YapePaymentScreen` al subir plan → ahora abre `PlanSelectorSheet` con MercadoPago (tarjeta/PagoEfectivo) + Yape; (c) el toggle "Mostrar Ofi" solo salía a clientes puros → ahora visible para todos: la preferencia controla el FAB de la home y en el panel del proveedor Ofi sigue siempre disponible (`AiAssistantFab._shouldShow` respeta la pref cuando `providerType==null`). Sin backend/SQL.
- **Simplificación UX móvil (PR #77, squash `7d42d14`, 2026-08-18):** dos superficies simplificadas para reducir la gestión del cliente. **Onboarding** (`welcome_screen.dart`): carrusel de bienvenida pasa de 7 a 4 slides — los 3 provider-cards estáticos (oficio/profesional/negocio) se fusionan en una sola tarjeta rotativa (`SlideProviderShowcase`, crossfade cada 2.8 s) y el grid de tipos se absorbe en el slide intro; quedan Bienvenido · Para cada necesidad · Testimonios · Ofi. Se eliminó `slide_types_grid.dart`. **Filtros avanzados** (`filter_sheet.dart`): las 3 acciones de aplicar (Aplicar filtros, Ampliar búsqueda, Buscar a la redonda) se unifican en un único botón inferior. La búsqueda por radio pasa a un toggle "Buscar por radio"; el `FilterRadarMap` perdió su botón propio y reporta centro/radio vía `onChanged`, y el botón único decide el modo (radar ON → `applyNearby`, OFF → `applyFilters`). El banner "Ampliar" solo ajusta estado sin auto-aplicar. Sin backend/SQL.
- **Coordenadas en registro** (PR #31): `RegisterProviderDto` acepta `latitude/longitude` opcionales y se persisten al crear el Provider (web las saca del enlace de Maps) — antes ningún cliente las enviaba y los proveedores nuevos no salían en búsqueda por radio. Trigger + backfill: `backend/prisma/sql/provider_location_geog_trigger.sql` (aplicado en Supabase).
- **Servicios Profesionales V1** (PR #55, squash `09c1711`, mergeado 2026-07-31): tercer tipo de proveedor `PROFESIONAL` (abogados, ingenieros, contadores — credenciales verificadas) junto a `OFICIO`/`NEGOCIO`. Invariante XOR: cliente + (OFICIO **o** PROFESIONAL, nunca ambos) + NEGOCIO opcional, forzado por índice único parcial en BD + validación de servicio. Un OFICIO migra a PROFESIONAL vía `professional-migrations` (admin revisa documentos), preservando el mismo `Provider.id` — nunca crea un Provider duplicado; la migración NO otorga planes de cortesía, no toca referidos ni cambia el rol del usuario. `submit()` exige `verificationStatus === 'APROBADO'` del Oficio antes de aceptar la solicitud (decisión explícita del propietario). El sello "Credenciales verificadas" solo aparece tras aprobación admin; especialidad es el único campo obligatorio del alta profesional. Categorías exclusivas por tipo (`pro-*` para Profesional). `ProviderType` (móvil, `core/utils/provider_type.dart`) agrega un valor `unknown` explícito — un string no reconocido ya NO se alias-ea silenciosamente a `oficio`; `'PROFESSIONAL'`/`'BUSINESS'` (inglés) siguen aceptándose como alias legacy de lectura únicamente (nunca se vuelven a emitir), canonicalizados en `ProvidersProvider.setType()` antes de llegar al backend. Auditoría de cierre completa (16 hallazgos de seguridad/pagos/categorías + UX móvil/web/admin + gaps de test, todos corregidos y probados) en `docs/AUDITORIA_SERVICIOS_PROFESIONALES_V1.md`. Los 6 SQL idempotentes (`profesionales_01..06_*.sql`) ya fueron aplicados manualmente en Supabase por el propietario.
- **Captación de leads NEGOCIO + conversión "Paso 6"** (PR #58, squash `aac6865`, mergeado 2026-08-10): pipeline para poblar Servi con negocios reales captados de Google Maps. El **scraper** vive en repo aparte `L3x-00/Servi-Scrapping` (panel local FastAPI + Playwright, Chromium visible, ≤5 consultas/lote y ≤20/consulta; extrae nombre/dirección/teléfono/categoría/horario/redes/fotos; RUC y WhatsApp manuales o por enlace explícito). Exporta `import.sql` a tablas de **staging** (`provider_lead_runs`/`provider_leads`/`provider_lead_photos`) con RLS + REVOKE; el SQL `provider_leads_staging.sql` lo aplica el propietario a mano en Supabase. **Cargar staging NO publica nada** (es privado, RLS). La **conversión** (`backend/src/lead-conversion/`) toma leads `CONSENTED` y crea `User` + `Provider(NEGOCIO)` reusando la validación real del registro (`validateProviderCategorySelection`, `uniqueSlug`, bcrypt), en PENDIENTE por defecto o aprobado/visible (+ `Subscription` de cortesía + rol PROVEEDOR). Idempotente por `convertedProviderId`; sube las fotos del lead a R2. Dos formas de correrla: (a) **botón "Captación" en el panel Admin** (PR #61 `cffc197`, endpoint admin-only `/admin/lead-conversion` — `LeadConversionModule` ahora SÍ está en `AppModule`, superficie de prod); (b) CLI local `npm run convert-leads` (dry-run por defecto, `--approve`/`--skip-photos`). Al convertir se **generan las credenciales** del negocio (PR #62 `94152d2`): email `<slug>@oficioapp.org.pe` (sufijo `-2/-3` solo si colisiona) + contraseña de 8 chars alfanuméricos que el admin ve **una sola vez** (modal con copiar; el backend solo guarda el hash bcrypt). Solo convierte con consentimiento humano (`CONSENTED`). **Fotos:** el scraper corrige que sean del negocio abierto y en HD (excluye la lista `role="feed"` y reescribe el tamaño a `=s1600`). La Opción A (auto-registro pre-cargado) quedó descartada: el tool ya crea la cuenta.

## 8. Skills y preflight de agentes

Claude usa `.claude/skills/`; Codex usa `.agents/skills/`. Encapsulan flujos
repetitivos para no re-derivarlos. Al cambiar una regla compartida, sincronizar
ambas variantes. Las reglas Servi de `/sql-prod` prevalecen sobre skills
genéricos Supabase/Postgres.

- **/servi-preflight** (Codex) — estado compacto: rama/diff, apps, gate SQL,
  contexto, Graphify, memoria, skills, artefactos, RTK y Node esperado.
- **/verificar** — checks por diff: tsc+jest backend, analyze+test móvil, admin/web. Correr tras cada cambio importante.
- **/servi-ai-release-audit** — auditoría reproducible de Ofi/WhatsApp: contenido curado, flags, frontera OpenWA, revisión Opus, validación `npm run ai:audit` y cierre seguro antes de publicar o activar.
- **/subir-pr** — rama → stage selectivo → commit → push → PR (gh REST) → poll CI → gate SQL → squash-merge → sync main.
- **/sql-prod** — protocolo BD: schema + prisma generate + SQL idempotente que el USUARIO aplica manualmente en Supabase (bloquea merge).
- **/ui-tema** — reglas de color/tema móvil (context.colors, tintOn/onSolid, const, tests con ThemeExtension). Leer ANTES de tocar UI Flutter.
- **/cerrar-tanda** — actualizar este doc (§7/§10) + memoria + reporte final tras mergear.

`web/` se construye dentro del status protegido **Admin (Next.js)**; `/verificar`
mantiene además el build local dirigido.

### Mapa documental

- `docs/CONTEXTO_PROYECTO.md`: fuente canónica de estado, stack, reglas y pendientes.
- `docs/ARQUITECTURA_DESPLIEGUE.md`: infraestructura, despliegue, resiliencia y BD.
- `docs/SEGURIDAD_OPERATIVA.md`: estado auditado y runbook de TLS, correo, MFA, Android, Supabase y origen Render; distingue verificación de aplicación.
- `docs/REACTIVACION_FUNCIONALIDADES_OCULTAS.md`: reactivación y rollback independiente de las cinco features ocultas y Carta/Catálogo para OFICIO.
- `docs/ESTADO_ACTUAL.md`: puntero corto a este documento; no duplica estado.
- `docs/AUDITORÍA_TÉCNICA.md`: snapshot histórico del 2026-06-02; no usar como estado vivo.
- `graphify-out/GRAPH_REPORT.md` y `graphify-out/graph.json`: mapa estructural; confiar solo si `npm run preflight` marca `FRESH`. La frescura compara cambios commiteados de fuente de apps; commits solo de docs/tooling no invalidan el grafo.

### Inicio de un chat nuevo

```text
Trabaja en C:\Users\Usuario\oficio_app. Antes de editar, ejecuta npm run preflight
y lee docs/CONTEXTO_PROYECTO.md. Preserva el árbol sucio y no toques mobile/
inconcluso sin briefing. Si Graphify está FRESH, consúltalo; si está STALE,
usa contexto canónico + rg y no lo regeneres sobre trabajo incompleto.
```

## 9. Comandos y verificación

```bash
# Backend (ESM)
cd backend && npm run start:dev          # dev :3000
npx tsc --noEmit                         # typecheck
npm test                                 # unit (743 pasando, 2026-08-13)
npm run test:integration                 # requiere stack Docker local
npm run test:e2e                         # requiere stack Docker local
npx cross-env RUN_GEMINI_CONTRACT_TESTS=true npm run test:contract  # Gemini real; requiere clave local
# Mobile
cd mobile && flutter analyze
cd mobile && flutter test                # 238 pasando (2026-08-13)
# Admin / Web
cd admin && npm run dev                  # :3001   (type-check aparte: next build ignora TS)
cd web && npm run build                  # :3002
# Infra local
docker-compose up -d
```

Pre-commit (husky + lint-staged): backend/admin con eslint/prettier y móvil con
`dart format`/`analyze`; web queda gateado por `next build` en CI.

Node objetivo = 20 (`.nvmrc` + GitHub Actions). Si `npm run preflight` reporta
Node 24 u otra versión local, cambiar a Node 20 (`nvm use 20`, `fnm use 20` o
equivalente) antes de instalar dependencias o construir apps Node.

Preflight: `npm run preflight`. RTK es opcional: si no existe en PATH, usar el
comando directo sin reintentar (ausente en PowerShell al 2026-07-11).

Web Next 16: `web/` debe usar `proxy.ts` (no `middleware.ts`), `metadataBase`
en `app/layout.tsx` y `turbopack.root` en `next.config.ts` para evitar warnings
de build en monorepo.

**Verificación local completa 2026-07-22, Node 20.20.2 / Flutter 3.41.6:**
backend 63 suites/571 unitarios, 19/101 integración sobre Postgres local, 2/16
E2E y 1/7 contratos Gemini reales; TypeScript, build y ESLint pasan. ESLint
conserva 262 warnings históricos y cero errores. Cobertura backend: 58.23%
statements, 50.68% branches, 43.65% functions y 58.80% lines. Jest todavía
emite el warning histórico de `forceExit`/handles abiertos después de pasar.
Mobile: 220/220 y cobertura 12.4% (3468/27873), superior al ratchet 9%; analyze
con 14 infos de estilo y cero warnings/errores. Admin: 9 archivos/25 tests,
type-check y build OK; cobertura 15.04% statements/lines, 69.66% branches y
41.29% functions. Su lint global conserva deuda previa de 84 errores/24
warnings; CI lo trata como informativo. Web no define tests; build y TypeScript
OK. No se construyó `.aab` por decisión del propietario. Producción y Supabase
no fueron modificados durante estos checks.

**Producción confirmada 2026-07-22:** PR #49 squash `db0d000`; CI Backend,
Mobile y Admin/Web verde. Render `GET /health` responde `ok` y el endpoint nuevo
de foto Admin sin JWT responde 401, comprobando runtime nuevo sin mutar datos.
Vercel web/admin completó deploy; web redirige al `www` y termina 200; Admin
expone headers defensivos y CSP `Report-Only`. Auditorías npm del 2026-07-16:
cero vulnerabilidades high/critical; moderadas conocidas backend 20, admin 10,
web 2 y una low admin, sin `--force` por riesgo de downgrades/cambios mayores.

**Verificación de la tanda Servicios Profesionales V1, 2026-07-31 (conteos
unitarios; no se repitió integración/E2E/contratos ni cobertura completa de
§ arriba):** backend 73 suites/638 unitarios verde; mobile 231/231 verde,
`flutter analyze` 14 infos de estilo preexistentes y cero warnings/errores;
admin 10 archivos/26 tests verde, `tsc --noEmit` limpio; web `npm run build`
(incluye TypeScript) limpio, 18 rutas generadas. **Producción confirmada:**
PR #55 squash `09c1711`; CI Backend/Mobile/Admin verde, Vercel preview
web/admin verde. Render `GET /health` responde 200 y
`GET /admin/professional-migrations` sin JWT responde 401, comprobando el
runtime nuevo desplegado sin mutar datos.

## 10. Estado / pendientes

- **2ª tanda de fixes web + Ofi (reporte del propietario, 2026-08-19):** 3 PRs, CI verde. **PR #101 (`18fe569`) — login + tema de modales:** (a) con credenciales inválidas la web **se recargaba** y había que reescribir — `apiFetch` trataba TODO 401 como "sesión expirada" (`clearSession()`+`location.href="/login"`), pero `/auth/login` devuelve 401 por credenciales inválidas; se añadió `skipAuthRedirect` a `apiFetch`, usado en `login`/`social-login` → el 401 vuelve como error normal y el form conserva lo escrito; (b) el modal de pago (Yape/MercadoPago) no adaptaba al tema — usa `bg-dark-surface/95` (con opacidad) y la inversión solo cubría la clase base; se añadieron overrides `html.light` para `bg-dark-surface/card` en sus opacidades. **PR #102 (`5ae8c61`) — visual:** botones flotantes reorganizados (Ofi abajo-derecha; WhatsApp+FAQ pareja abajo-izquierda, antes FAQ estaba aislado a la izq-centro) y las tarjetas de `/buscar` ahora tienen **carrusel automático** de imágenes (`CardImageCarousel`, crossfade + puntos, arranque escalonado por id). **PR #103 (`04f47bc`) — Ofi soporte:** al pedir asistente/soporte o cómo contactar a Servi, Ofi respondía `[DATO PRIVADO]` (la frase no activaba el router determinista → caía al modelo → el guardrail redactaba el número/correo); se amplió `isSupportRequest` (frases de contacto con Servi, con regla guardada que excluye "proveedor") → entrega los canales oficiales (WhatsApp+correo) como acciones; además `SALES_EMAIL` deja de exponer el correo personal → oficial. `admin-router.spec` 10/10. Render redeploya.
- **Fixes + rediseño del panel proveedor (reporte del propietario, 2026-08-19):** 3 PRs, CI verde, solo `web/`, sin backend/SQL. **PR #97 (`99a8b77`) — bugs de datos:** (a) Estadísticas mostraba `Reseñas: 0` aunque hubiera reseñas — la card leía `analytics.totalReviews`, pero `getMyAnalytics` **solo devuelve clics/vistas** (nunca reseñas); la fuente real es el perfil (`provider.totalReviews`/`averageRating`, como en el móvil) → ahora usa eso y muestra el promedio; (b) el campo Teléfono del perfil salía vacío porque `provider.phone` suele estar en blanco (muchos solo llenan WhatsApp) → fallback a `user.phone` (que `GET /me` ya devuelve; se añadió `user` al tipo `Provider` del web). **PR #98 (`bc6db83`) — reorganización de Perfil:** 3 secciones separadas (Plan/Disponibilidad/Verificación) + colapsable "Planes" → **una sola tarjeta de estado compacta** (fila plan + fila con disponibilidad **segmentada** y verificación como **badge**), con la grilla de planes integrada y colapsable (`AnimatePresence` de alto) desde "Cambiar plan" → mucho menos scroll. **PR #99 (`30cc1b9`) — tema + transición:** la inversión global `html.light` cubría solo una **lista fija de opacidades**; las intermedias que faltaban (`text-white/20`,`/55`, `bg-white/[0.02]`,`/[0.06]`,`/[0.08]`, `border-white/30`…) quedaban blancas en modo claro → Mensajes y **AccountMenu** invisibles; se completaron las sutiles/de texto (los `bg-white` 60-90 se dejan por si son sólidos intencionales). Además la transición entre secciones del panel pasó de `mode="wait"` exit 0.2s+enter 0.3s a exit 0.12s/enter 0.22s (se sentía "demorada"). **Nota:** la dirección del perfil (`provider.address`) se lee directo como el móvil; si aparece vacía, no está seteada a nivel de proveedor (el usuario puede escribirla).
- **Pulido visual del panel proveedor mergeado (2026-08-19):** PR #95, squash `49adb69`; CI verde, solo `web/`, sin backend/SQL. Auditoría: el panel ya era de alta calidad (shell responsivo, sidebar colapsable con indicador `layoutId`, transiciones de página, skeletons, count-up). Único defecto visual real: los **charts de `/panel/estadisticas` (recharts)** tenían ejes/grid/tooltip hardcodeados en dark vía **props/estilos inline sobre SVG** — la inversión global `html.light` no los alcanza (mismo caso que los toasts sonner) → en modo **claro** los ejes salían blancos-sobre-blanco (invisibles) y el tooltip oscuro. Nuevo hook `web/lib/chart-theme.ts` (`useChartTheme`) resuelve ejes/grid/tooltip/cursor por tema; series de marca (terracota/cian) intactas. Además: empty state de gráficos con icono+copy y **sidebar con colapsado persistente** (`localStorage`, antes se re-expandía en cada navegación). `components/stats-charts.tsx` tiene el mismo patrón pero **está sin usar** (dead code) — no se tocó. La deuda del panel dark-hardcoded + inversión global SIGUE (migrar a `dark:` es alto-churn); lo que se corrigió aquí es solo lo que la inversión NO cubre (SVG inline).
- **Tanda seguridad web mergeada (2026-08-19):** PR #93, squash `aee0064`; CI verde, solo `web/next.config.ts`, sin backend/SQL ni cambios de auth/runtime. Auditoría: base ya sólida (cookie `SameSite=Lax`+`Secure`, `X-Frame-Options: DENY`, `X-Content-Type-Options`, `Referrer-Policy`; **sin** `dangerouslySetInnerHTML`; todos los `target=_blank` con `rel=noopener`). Se añaden las 3 cabeceras que faltaban: **`Content-Security-Policy-Report-Only`** (mismo criterio que el panel admin — reporta sin bloquear; `connect-src` acotado al backend REST+`wss`, `img-src` amplio para R2/CDN/tiles, `script/style` `'unsafe-inline'` por Next), **`Permissions-Policy`** (solo `geolocation=(self)`; camera/mic/payment/usb/sensores denegados) y **HSTS** (`max-age` 1 año, sin includeSubDomains/preload). Verificado emitiéndose con `next start`+`curl`. **Pendiente del propietario (opcional):** tras observar reportes CSP sin falsos positivos, cambiar la clave a `Content-Security-Policy` (enforcing) en un PR de 1 línea. El token sigue client-side (JWT localStorage + cookie no-httpOnly que lee el middleware); migrar a httpOnly requiere cambios de auth en backend (fuera de tanda). **Con esto la auditoría web queda COMPLETA (T1/T2/T3 + modales + seguridad).**
- **T2 hub del cliente mergeado (2026-08-19):** PR #90, squash `1d66e6a`; CI verde, solo `web/`, sin backend/SQL (el backend ya soportaba todo). Bugs funcionales de `/cliente`: (1) el botón de **quitar favorito** (corazón) estaba **inerte** (sin `onClick`) → ahora togglea `POST /favorites/:providerId`, quita optimista y revierte si falla; las tarjetas ahora enlazan al perfil público (`/:id`). (2) Las **notificaciones nunca se marcaban leídas** (la vista jamás llamaba al endpoint) → al abrir la pestaña con pendientes se llama a `PATCH /provider-profile/me/notifications/read-all` (scope por userId) y se refleja local. (3) **Deep-link `?tab=`** solo reconocía `referidos` → ahora mapea favoritos/notificaciones/ajustes/referidos. Pulido de movimiento: eliminación animada de favoritos (`AnimatePresence popLayout`), tarjetas clicables, stagger en notificaciones. Verificación: web build Node 20 OK.
- **Pulido de modales/transiciones mergeado (2026-08-19):** PR #91, squash `71b69e4`; CI verde, solo `web/`. **Toasts según el tema**: nuevo `web/components/themed-toaster.tsx` lee `next-themes` y aplica glass claro/oscuro — antes el `<Toaster>` estaba fijo en `theme="dark"` y chocaba en modo claro (sonner pinta en un portal con estilos inline, la inversión global `html.light` no lo cubría). Modal de planes: transición deslizante entre pasos plans↔método (`AnimatePresence` direccional) + `exit` del panel. Sin backend/SQL.
- **Auditoría web + T1 funcional mergeada (2026-08-19):** PR #87, squash `18930b6`; CI Backend/Mobile/Admin verde + previews Vercel OK. Tras mapear/auditar la web por rol, primera tanda de bugs funcionales del panel/cliente: **servicios se guardan de verdad** (antes `handleSave`=`toast('...(simulado)')`) — CRUD sobre `scheduleJson.services` vía `PATCH /provider-profile/me` (mismo shape/endpoint que el móvil `ServiceItem`, preserva el resto del scheduleJson) con imagen opcional (`/upload/provider-photo`), precio y unidad; límites reales por plan (espejo de `plan_limits.dart`: OFICIO 1/6/∞, NEGOCIO 3/6/∞). Fotos: `uploadImage`/`deleteImage` envían `?type=` (perfil activo) y el límite de galería es por plan (2/6/10) en vez de 5 fijo. **Portada elegible**: endpoint backend nuevo `PATCH /provider-profile/me/images/:id/cover` (ownership por userId, única portada por transacción; la columna `isCover` ya existía → **sin SQL**) + botón al hover en el panel; badge de portada único (antes se duplicaba con `idx===0`). Disponibilidad usa `PATCH /me/availability` (antes iba por `PATCH /me` → el DTO rechaza el campo → **400**). Radar de `/buscar` renderiza basemap CARTO (faltaba `<TileLayer>`, salía en blanco). Modal Yape subido a `z-[100010]` (quedaba detrás del de planes `z-[100000]`). `/payments/success` refleja el `status` real de Mercado Pago (aprobado/pendiente/rechazado/confirmando) en vez de afirmar "exitoso" siempre. `plans.ts` copia de fotos 2/6/10 (antes 4/ilimitadas). Verificación: web `next build` Node 20 OK (18 rutas), backend tsc limpio, `provider-profile.service.spec` **25/25** (+2 de `setCoverImage`). Backend Render redeploya (endpoint aditivo). **Pendiente del propietario:** ninguno de SQL; el panel web ya sirve la funcionalidad al publicarse Vercel.
- **T3 pulido visual/UX cliente mergeado (2026-08-19):** PR #88, squash `910c4f8`; CI verde. Pase de refinamiento (la web ya es sólida: identidad cálida-artesanal, tema claro/oscuro, focus-visible global, reduced-motion — se eleva, no se rehace). Se **retira el correo personal** `ronla.angarita31@gmail.com` expuesto en footer ("Ventas y planes") y manual → oficial `soporteofiapp@gmail.com`. `ProviderCard` (resultados/carruseles) muestra **disponibilidad** (punto de color) y **sello de credenciales** para PROFESIONAL, con datos que el backend ya envía (render condicional). `/buscar`: estado vacío útil (icono+sugerencia+acción) y **skeleton-grid** (`aria-busy`, sin CLS) en vez de spinner. Perfil público: badge Premium con icono lucide `Crown` (antes emoji `⭐`). Solo `web/`, sin backend/SQL. **Deuda anotada (no bloqueante):** el panel del proveedor es dark-hardcoded + inversión global de tema (`html.light` en `globals.css`) — funciona pero migrarlo a variantes `dark:` es un cambio grande/riesgoso; se difiere.
- **Fix build `.aab` + mejoras post-auditoría (2026-08-18):** PR #85, squash `60e5329`; CI 3 checks verdes. **Build `.aab`:** `flutter_native_splash` movido de `dev_dependencies` a `dependencies` — declara un plugin Android que el `GeneratedPluginRegistrant` registra siempre, pero Flutter 3.41.x excluye el módulo nativo de las dev_dependencies en RELEASE → `package net.jonhanson.flutter_native_splash does not exist` al generar el bundle. Verificado con `flutter build appbundle --release` (app-release.aab 69.4 MB, exit 0). **NO volver a moverlo a dev_dependencies.** **Mejoras:** el radar del filtro respeta la disponibilidad activa (backend `getNearby`+controller + móvil `applyNearby` con centinela; `sortBy` no aplica por orden de distancia, `verified` es no-op latente también en `findAll`); `PlanSelectorSheet` recibe `initialPlan` y auto-scrollea a la tarjeta tocada desde Ajustes; `offer_form_sheet` usa `resolvePanelType`; slide 2 del onboarding con fondo neutro; copy del diálogo "Ampliar búsqueda" aclarado; indicador de cambios del filtro reacciona al radio del radar. Refactor completo `isNegocio`→`providerType` en los constructores de tabs quedó **diferido** (alto-churn; `resolvePanelType` ya cubre todos los sitios activos). Verificación: `flutter analyze` limpio, `flutter test` 244/244, backend tsc sin errores nuevos, `providers.service.spec` 34/34. Sin schema/SQL.
- **Auditoría de sesión 2026-08-18 (PRs #77/#79/#81 + fix #83):** revisión general de los cambios de la sesión. Único bug encontrado y corregido: el FAB de Ofi del panel pasaba `widget.providerType ?? auth.activeProfileType` (puede ser null); con el `_shouldShow` de PR #79 (panel muestra Ofi solo si `providerType != null`) un null habría dejado que el toggle "Mostrar Ofi" ocultara Ofi también en el panel. PR #83 (squash `cf19493`) pasa `activeType` (nunca null) + test de regresión. CI 3 checks verdes; `flutter test` 244/244. Resto sin bugs: slides 4/filtro (PR #77), panel PROFESIONAL/pagos (PR #79) y enlace FB (PR #81) funcionan; el chip "Revisión pendiente" del Yape-en-selector se hidrata vía `loadDashboard`. Mejoras sugeridas (no bloqueantes): (1) refactor para pasar el `providerType` real por los tabs en vez del bool `isNegocio` — la raíz que causó el bug PROFESIONAL; `resolvePanelType` es un stopgap; (2) `offer_form_sheet` (feature OCULTA) aún usa el binario `isNegocio?'NEGOCIO':'OFICIO'` — arreglar al reactivar ofertas; (3) el modo radar del filtro ignora availability/verified/sort; (4) fondo del slide 2 (foto oficio) no rota con la card oficio/prof/negocio; (5) el diálogo "Ampliar búsqueda" sugiere efecto inmediato pero ahora requiere el botón único Aplicar.
- **Enlace global de Facebook unificado (2026-08-18):** PR #81, squash `2f4d988`; CI 3 checks verdes. Las 4 superficies con el enlace oficial (footer web, fallback de `email-templates`, `contact_us_section` móvil, `servi.md`) estaban inconsistentes; ahora todas apuntan a `https://www.facebook.com/share/1PtKTQ95DA/`. No afecta a Ofi (no se compila en `servi-platform-knowledge.ts`). Sin schema/SQL. **Decisión:** no se usa env `SOCIAL_FACEBOOK_URL` en Render — solo el correo la leería y web/móvil son hardcode, así que se mantiene el código como fuente única para evitar drift. Móvil llega a usuarios al recompilar el `.aab`.
- **Fix panel PROFESIONAL + pagos + Ofi mergeado (2026-08-18):** PR #79, squash `4702be7`; CI Backend/Mobile/Admin verde + previews Vercel OK. Raíz común de dos síntomas reportados por un proveedor migrado a PROFESIONAL: los tabs del panel usaban `isNegocio ? 'NEGOCIO' : 'OFICIO'` e ignoraban PROFESIONAL → 404 en cancelar/analytics/perfil y confianza en "solicitar". Fix con helper `resolvePanelType` (XOR de perfiles). Además: error de cancelar amigable (AppException tipada, ya no `$e` crudo), panel abre `PlanSelectorSheet` (MercadoPago+Yape) al subir plan, y toggle "Mostrar Ofi" visible para todos (controla el FAB de la home; panel siempre muestra Ofi). Verificación local: `flutter analyze` limpio y `flutter test` 243/243 (incluye regresión nueva `panel_provider_type_test`). Sin backend/SQL → Render no cambia. **Pendiente del propietario:** recompilar/publicar el `.aab` para que llegue a usuarios.
- **Simplificación UX móvil mergeada (2026-08-18):** PR #77, squash `7d42d14`; CI Backend/Mobile/Admin verde + previews Vercel OK. Onboarding 7→4 slides (card rotativa `SlideProviderShowcase`, se eliminó `slide_types_grid.dart`) y filtros avanzados unificados a un botón con toggle "Buscar por radio" (`FilterRadarMap` reporta por `onChanged`, sin botón propio). Verificación local: `flutter analyze` limpio en los archivos tocados y `flutter test` 240/240. Sin backend, schema ni SQL → sin gate SQL; Render no cambia. **Pendiente del propietario:** recompilar/publicar el `.aab` para que la simplificación llegue a usuarios.
- **Checkout Mercado Pago corregido (2026-08-13):** PR #75, squash `27e7646`. El pago rechazado `173483733036` fue devuelto por Mercado Pago; no hubo rechazo interno de Servi. Cada intento nuevo obtiene `external_reference` único, el móvil consulta solo su intento y plan esperado al volver de Mercado Pago, y el backend muestra estado/rechazo seguro sin datos de tarjeta. El webhook aprobado sigue siendo la única vía que activa la suscripción. Incluye aviso de rechazo, deduplicado y telemetría segura. No requiere SQL ni variables nuevas. Pendiente operativo: rotar el access token expuesto en diagnóstico local y, en Mercado Pago, retirar webhooks antiguos o alinear su secreto antes del smoke aprobado/rechazado.
- **Asistente WhatsApp F1–F4 mergeado, pendiente de activación (2026-08-13):** PR #69, squash `0993337`; CI Backend/Mobile/Admin y previews Vercel verdes. Verificación local: Prisma format/generate, `tsc`, build Nest, ESLint y 84 unitarias focalizadas. SQL F1/F3/F4 aplicado manualmente y F0 OpenWA confirmado por el propietario. Render `GET /health` = 200 y el webhook F1 con firma falsa devolvió 204 mientras `WHATSAPP_ASSISTANT_ENABLED=false`. Pendiente de autorización explícita: activar F1 y ejecutar en número de staging `FAQ → STOP → silencio`, `HUMANO → una confirmación → silencio` y firma inválida → 401; después habilitar F2, F3 y F4 por separado. No probar con un chat real hasta esa autorización.
- **Auditoría Ofi + WhatsApp previa a activación (2026-08-13):** `docs/AUDITORIA_ASISTENTE_WHATSAPP_OFI_2026-08-13.md` documenta frontera OpenWA, privacidad, flags, herramienta pública Ofi y regresión de toda la plataforma. Corrige carrera F3, F3 apagada, falso handover, consumo de cuota por input bloqueado, KB dinámica ilimitada y reemplazo IA de mensajes de marca. `servi.md` queda como fuente humana versionada; Ofi usa solo contexto curado con web/Android oficiales. Verificación local: backend 84/743, admin 26, web build y móvil 238/238 verdes; analyze móvil tiene 14 infos preexistentes y cero errores. **Sigue pendiente únicamente el smoke real autorizado; no hubo SQL, activación, salida OpenWA ni mutación de producción.**
- **Captación en el panel Admin + credenciales al convertir (2026-08-11):** PR #61 (squash `cffc197`)
  agregó la pantalla **"Captación"** al admin (lista los leads del staging y los convierte con un botón,
  toggle "Aprobar y publicar"); esto metió `LeadConversionModule` en `AppModule` → **el endpoint
  `/admin/lead-conversion` es superficie de prod** (admin-only). PR #62 (squash `94152d2`): al convertir
  se generan las credenciales del negocio (email `<slug>@oficioapp.org.pe` + contraseña de 8 chars vista
  una vez, con modal de copiar; solo se guarda el hash). CI Backend/Mobile/Admin verde en ambos. Sin
  cambios de schema (staging se lee por SQL crudo) → sin gate SQL. Verificación: backend tsc + 651 tests,
  admin build + tsc, smoke E2E del service contra Postgres local. El **fix de fotos** (una por negocio +
  HD) vive en el scraper `Servi-Scrapping` (`27dec06`, junto a los botones de consentimiento del panel);
  **pendiente del propietario:** reiniciar el panel, re-scrapear y confirmar en un smoke de 1-2 fichas que
  las fotos son del negocio correcto (no pude probar el DOM de Maps en vivo).
- **Captación + conversión de leads NEGOCIO mergeada (2026-08-10):** PR #58, squash `aac6865`, CI
  Backend/Mobile/Admin verde, **sin gate SQL** (el staging no es modelo Prisma; se lee por SQL crudo).
  Herramienta local `backend/src/lead-conversion/` + `scripts/convert-leads.ts`, **no importada en
  `AppModule`** → el runtime desplegado en Render no cambia (Render redeploya el commit pero el módulo es
  inerte). Verificación: tsc limpio, backend **74 suites / 647 tests**, y smoke local end-to-end contra
  Postgres docker (dry-run → `--commit` PENDIENTE → `--approve` visible+Subscription+rol → idempotencia
  0 candidatos → foto descargada y subida a MinIO local con `ProviderImage` de portada). **Estado SQL:**
  el propietario ya aplicó `provider_leads_staging.sql` (staging + RLS/REVOKE + `convertedProviderId`) en
  Supabase. **Pendiente del propietario:** cargar el `import.sql` del panel y correr la conversión en real
  — apuntar `DATABASE_URL` a Supabase (el `.env` local es docker; `MINIO_*` ya es R2 de prod → **dry-run
  primero**, `--commit` crea Users/Providers reales). El scraper se pusheó a `L3x-00/Servi-Scrapping`
  (repo separado, worktree de `D:/servi-marketing`); su `data/` (SQLite + fotos, PII) y `.venv` nunca se
  versionan.
- **Servicios Profesionales V1 mergeado y verificado (2026-07-31):** PR #55, squash `09c1711`, 132
  archivos, CI Backend/Mobile/Admin verde. Fases 0-4 + auditoría de cierre completa (16 hallazgos de
  seguridad/pagos/categorías + UX móvil/web/admin + gaps de test, todos corregidos y probados — detalle
  en `docs/AUDITORIA_SERVICIOS_PROFESIONALES_V1.md`) + 4 regresiones de tests/fixtures desactualizados
  detectadas y corregidas en la verificación final. Verificación previa al merge: backend 73 suites/638
  tests, mobile 231/231 + analyze limpio, admin 26 tests + tsc limpio, web build+TS limpio. Los 6 SQL
  idempotentes `profesionales_01..06_*.sql` fueron aplicados manualmente en Supabase por el propietario
  (gate SQL cumplido antes del squash-merge). Post-merge: Render respondió `GET /health` 200 y
  `GET /admin/professional-migrations` sin JWT respondió 401 (confirma el runtime nuevo desplegado sin
  mutar datos); Vercel web/admin con preview verde en el PR. Gaps de cobertura conocidos, no
  bloqueantes: sin test del handler de socket `PROFESSIONAL_MIGRATION_APPROVED/REJECTED` (móvil no tiene
  infraestructura de socket falso todavía); `web/` sigue sin infraestructura de test (cero
  vitest/RTL/jsdom) — el bug crítico de crash del panel que se corrigió ahí no tiene regresión propia.
- **Desplegado antes de PR #49:** todo el backlog OBSERVACIONES (10 ítems, PRs #20–#25), correos (#26), tema adaptativo + back-panel (#27), Alcance por distritos (#28, SQL aplicado), landing web + wizard registro + coords en registro (#31, SQL trigger aplicado), descarga QR Yape migrada a `gal` + `file_paths.xml` faltante (#33). Trigger `subscription_audit_log` aplicado y versionado en `backend/prisma/sql/audit_log.sql` (#34) — lo usa `payments.service` vía GUC `app.current_user_id`. **Reducción de superficie:** subastas, ofertas, referidos/monedas, agenda y cotización OCULTAS; Carta/Catálogo restringidos a NEGOCIO. PlanRequest deprecado (endpoint vivo para apps viejas; tabla conservada), retención previa de notifs leídas 7d / no leídas 30d, fix fila histórica de payments en primer pago Yape y fix `markNotificationRead` admin sin scope.
- **Mergeado y verificado:** PR #37, squash `002791d` (2026-07-12), ocultó referidos/monedas, agenda y cotización en backend, móvil, web y Ofi sin borrar código. El usuario aplicó manualmente `backend/prisma/sql/ocultar_features_kb.sql` en Supabase: solo desactiva la entrada de conocimiento de Ofi; no cambia schema ni elimina triggers/funciones. El 2026-07-13 se verificaron Render sano, flags públicos apagados, CI verde y deploys Vercel web/admin exitosos.
- **Release consolidado 2026-07 desplegado:** PR #49, squash `db0d000`, apiló PRs #39–#48. Incluye hardening 7A–7F, runtime Next 16, UX autorizada web/móvil, errores de auth manual, retención 5/30 días, overlays globales de aprobación/rechazo, ciclo de notificaciones Admin, ocultamiento Admin de Referidos/Recompensas y alta única de foto faltante. Render/Vercel y CI verificados el 2026-07-22. No hubo schema, SQL ni ejecución contra Supabase.
- **PR #53 mergeado 2026-07-29:** squash `c4c275e`. Ofi gana identidad, formato seguro, soporte directo, interfaz web opaca/adaptativa, conversación 30/12 h y reinicio explícito; memoria interna y aprendizaje global agregado. El usuario aplicó manualmente los SQL idempotentes `actualizar_soporte_ofi.sql`, `ai_memory_governance.sql` y `ai_learning_candidates.sql` en Supabase. También incluye deduplicación de proveedores en `/buscar`, navegación/panel web unificados, bloqueo de scroll en modales y enlaces públicos cortos desde móvil. CI Backend, Admin y Mobile verde; Vercel web/admin verde. Render no ejecutó deploy por gate SQL esperado; verificar su ciclo automático antes de declarar runtime actualizado.
- **Hardening 7A–7F versionado en PR #39 e integrado en #49:** subidas autenticadas con límites, decodificación/re-encode Sharp y descarte de metadatos; validación de origen/carpeta para URLs gestionadas; DTO estricto para perfil propio, guards/roles y privacidad de perfiles públicos; paginación acotada; login social exige proveedor confiable y `email_verified`, sin vincular UIDs distintos por email; usuarios suspendidos pierden refresh tokens y WebSocket revalida usuario/rol contra BD; CSV neutraliza fórmulas; fallback de correo no registra OTP/URLs/destinatarios; admin agrega headers defensivos y CSP `Report-Only`. No incluye certificate pinning, WAF agresivo ni controles externos.
- **Graphify 2026-08-13:** regenerado desde `main` limpio en `27e7646` tras PR #75: 1,098 archivos, 10,072 nodos, 16,528 relaciones y 447 comunidades (366 mostradas; 81 delgadas omitidas). Extracción 100%; 75 relaciones inferidas con confianza promedio 0.80. Incluye el seguimiento de checkout Mercado Pago, `whatsapp-assistant` F1–F4 y el catálogo recuperable de Ofi. `graph.html` no se genera porque supera el límite visual de 5,000 nodos; usar `GRAPH_REPORT.md`, `graph.json` y `graphify query`. El grafo excluye cambios locales sin commit; commits solo documentales/tooling no invalidan su frescura.
- **Limpieza local 2026-08-12:** artefactos generados, notas locales y el worktree huérfano de `Servi-Scrapping` fueron retirados de la raíz de Servi. Respaldo remoto: `codex/respaldo-limpieza-local-20260812`. Configuración privada de agentes queda fuera de Git. Nunca usar `git add -A`.
- **Seguridad operativa pendiente:** ejecutar manualmente y por separado `docs/SEGURIDAD_OPERATIVA.md`: rotar la cuenta de prueba expuesta y revocar sesiones; fijar TLS mínimo 1.2 en Cloudflare; configurar SPF/DKIM y luego DMARC en observación; exigir MFA; confirmar integraciones externas antes de apagar Supabase Data API; habilitar SSL obligatorio de Postgres solo en ventana porque reinicia la BD; no bloquear todavía el origen directo de Render. Estado documentado no significa aplicado.
- **Pendiente manual:** recompilar/publicar `.aab` queda a cargo del usuario. Smoke autenticado Admin de filtro de notificaciones y alta de foto sobre proveedor sin imágenes requiere cuenta Admin; no se ejecutó con credenciales de cliente. Móvil aún NO envía lat/lng al registrar (DTO ya acepta; adopción pendiente). CORS prod (`ALLOWED_ORIGINS`) no incluye `localhost:3002`: integración web local debe usar proxy. `test:cov` modifica artefactos versionados de `coverage/`; no stagearlos. Limpieza técnica de settings/coverage/generated queda en PR separado. El `AuditLog @@map("audit_log")` + `AuditLogService` de auditoría V2 nunca se commiteó; no confundir con trigger #34.
- **Docs:** fuente viva `docs/CONTEXTO_PROYECTO.md`; arquitectura en `docs/ARQUITECTURA_DESPLIEGUE.md`; seguridad operativa en `docs/SEGURIDAD_OPERATIVA.md`; reactivación en `docs/REACTIVACION_FUNCIONALIDADES_OCULTAS.md`; reporte de esta tanda en `docs/REPORTE_RELEASE_2026-07-22.md`; `docs/ESTADO_ACTUAL.md` es puntero y `docs/AUDITORÍA_TÉCNICA.md` es histórico.
